ALTER TABLE forums ADD gameID int;
UPDATE forums c LEFT JOIN forums p ON c.heritage LIKE CONCAT(p.heritage, '%') LEFT JOIN games g ON p.forumID = g.forumID SET c.gameID = g.gameID WHERE p.parentID = 2 AND p.forumID != 10;
